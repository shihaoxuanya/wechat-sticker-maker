# 一张图做微信表情包

把一张人物、宠物或角色参考图，变成默认24款高清静态表情及封面、横幅、聊天图标，并整理逐项上传清单。可以指定数量、表情名字、画风和是否加文字，也可以检查已有表情或根据驳回截图修正。

## 安装

1. 解压 `wechat-sticker-maker.zip`。
2. 将整个 `wechat-sticker-maker` 文件夹放入你的 Codex skills 目录：Windows 通常是 `%USERPROFILE%\.codex\skills\`；macOS/Linux 通常是 `~/.codex/skills/`。若设置了 `CODEX_HOME`，使用其中的 `skills` 目录。最终结构应为 `skills/wechat-sticker-maker/SKILL.md`，不要多套一层同名目录。
3. 在能使用内置图像生成能力的 Codex 环境中调用。当前这台电脑已经安装，无需重复复制。技能列表若未更新，可在新任务中调用它。

配套处理工具需要 Python 3.10+、Pillow 和 NumPy。Codex可优先使用已有环境；手动安装依赖时，建议使用独立虚拟环境，并执行 `python -m pip install -r wechat-sticker-maker/scripts/requirements.txt`。仅整理已有素材时不需要重新生成图片；工具本身不使用收费图像API密钥。

## 开始使用

上传一张参考图，然后输入：

> 请使用 $wechat-sticker-maker，根据这张图做24款微信静态表情。保持角色一致，突出头脸，适合的图片配自然手写字。请同时制作封面、横幅和独立聊天图标，检查素材并给我上传清单。

也可以更具体：

> 用 $wechat-sticker-maker 做16款无字表情，包含亲亲、抱抱、生气和困困，剩下的你安排。

> 请用 $wechat-sticker-maker 检查这个已有表情包目录，先告诉我哪些文件不符合当前投稿要求。

> 这是平台驳回截图，请用 $wechat-sticker-maker 修正涉及的图片，更新预览和压缩包，并指出需要重新上传哪几个文件。

## 文件怎么用

- `highres`：1024像素以上的高清母图；`png` 和 `中文名称版`：日常使用图片。
- `submission`：准备上传的主图、缩略图和配套素材；具体位置按 `upload-guide.md` 选择。
- `preview.html`、`qa`：整套预览、浅深背景和原尺寸图标检查图。检查图不要上传成表情。
- `packages`：完整包与投稿素材包。ZIP是交付容器，平台未明确支持时先解压再选文件。

聊天图标必须使用独立图标文件，不要拿带字表情、封面或放大检查图替代。

## 规则与检查状态

内置参数是制作基线，并非已经核实的最新微信标准。每次准备投稿时，skill会核对官方页面或你提供的官方规范截图，分清硬性限制、自动压缩阈值和设计建议。无法访问或证据不全时仍可制作素材，但压缩包会标注“草稿”并列出待办。

版权署名、肖像/素材授权、AI素材信息由你按实际情况确认。脚本只能测量文件；错字、手部、角色一致性、图标装饰和内容要求仍需实际看图与核对。它不能承诺一次审核成功，也不会代你登录、上传或提交。

分享包只含skill、工具、参考文档和合成测试，不含制作案例原图、私人聊天截图或账户信息。
